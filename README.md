# Матчинг товарных карточек — GBM-пайплайн

Решение задачи классификации пар товаров-кандидатов (ecup26-matching). Пары
собраны заранее, retrieval-часть решать не нужно — требуется для каждой пары
$(i, j)$ выдать $\hat{p}_{ij} \in [0, 1]$, вероятность того, что это одна и та
же товарная позиция.

Метрика соревнования — **PR-AUC, посчитанный отдельно по каждой из 20
категорий и усреднённый** (§5.3).

| Решение | Валидация | Лидерборд |
|---|---:|---:|
| Только строковые признаки | 0.5565 | 0.2650 |
| + MiniLM-L12, претрейн 2M пар | 0.7060 | 0.3626 |
| + MiniLM-L12, претрейн 11.19M пар | 0.7417 | 0.4245 |
| **+ e5-base, претрейн 11.19M пар** | **0.7827** | **0.4592** |

## Архитектура

```
пара (i, j)
   ├─ 19 признаков: строковое сходство названий, числа, транслитерация,
   │  атрибуты, идентификаторы, код категории                      (§2)
   └─ логит cross-encoder'а                                        (§7)
         ├─ предобучение на 11.19 млн LLM-размеченных пар, мягкие метки
         └─ дообучение на 292 тыс. пар ручной разметки
                                    ↓
              HistGradientBoostingClassifier               (§4)
                                    ↓
                          вероятность дубля
```

Четыре вывода, определивших решение, — все получены измерением, и три из них
опровергли то, с чего пайплайн начинался:

1. **Бюджет инференса вчетверо больше, чем нужно** (§7.1). Начинали с посылки
   «скорость — жёсткое ограничение, поэтому только дешёвые признаки, никакой
   нейросети». Замер показал ~170–195 с против лимита в 780 с при полностью
   простаивающем GPU. Отказ от нейросети стоил около $0.13$ ROC-AUC и не
   покупал ничего.
2. **LLM-разметка ценна, но не как обучающие строки** (§6.2). Подмешанная к
   ручной разметке, она качество *ухудшает* — монотонно, вплоть до
   вырожденного случая «LLM нет вовсе». Вынесенная в отдельную стадию
   предобучения — даёт крупнейший прирост за всю работу.
3. **Оптимизировали не ту метрику** (§5.3). Первые двадцать решений
   принимались по общему ROC-AUC, тогда как оценивается макро-среднее PR-AUC
   по категориям. Это разные величины: общая взвешивает категорию по числу
   пар, макро — уравнивает все двадцать.
4. **Валидация занижает пользу претрейна** (§6.3). Переход на претрейн 11M дал
   на валидации +5.5%, а на лидерборде +17%. Валидация состоит из товаров того
   же узкого подмножества, на котором шло дообучение, поэтому не видит
   основного эффекта — способности обобщаться на незнакомые товары.

---

## 1. Постановка

Пусть $x_i = (\text{name}_i, \text{attributes}_i, \text{category}_i)$ — карточка
товара. Требуется построить

$$f: (x_i, x_j) \mapsto \hat{p}_{ij} \in [0,1].$$

Обучающая выборка размечена из двух источников с принципиально разной природой:

| Источник | Объём | Природа $t$ | Смысл |
|---|---:|---|---|
| `matches.parquet` | 365 654 | $t \in \{0, 1\}$ | ручная разметка, ground truth |
| `matches_llm.parquet` | 11 187 780 | $t \in [0, 1]$ | вероятностная разметка LLM |

Каталог: `items.parquet` — 13 397 761 товар, 20 категорий;
`items_human.parquet` — 711 304 товара (подмножество, покрывающее ручные пары).

Асимметрия $\approx 30{:}1$ в пользу LLM-разметки — центральный факт, вокруг
которого построена вся методология валидации ниже (§5).

---

## 2. Признаки

17 признаков на пару, все — из стандартной библиотеки Python, без внешних
зависимостей. Обозначения: $s_1, s_2$ — строки названий,
$T_1, T_2$ — множества токенов названий, $A_1, A_2$ — словари атрибутов,
$K_1 = \operatorname{dom} A_1$, $K_2 = \operatorname{dom} A_2$ — множества
ключей атрибутов, $V_1, V_2$ — множества токенов значений атрибутов.

Токенизация: `[a-zA-Zа-яА-ЯёЁ0-9]+` после приведения к нижнему регистру.

### 2.1 Признаки по названию

**`name_seq_ratio`** — коэффициент Рэтклиффа—Обершелпа:

$$\text{ratio}(s_1, s_2) = \frac{2M}{|s_1| + |s_2|},$$

где $M$ — суммарная длина всех совпадающих блоков, найденных рекурсивным
алгоритмом наибольшей общей подстроки (`difflib.SequenceMatcher`). В отличие от
расстояния Левенштейна, учитывает порядок символов и устойчив к перестановке
крупных блоков — что важно для товарных названий вида
`«бренд артикул тип»` vs `«тип бренд артикул»`.

Сложность — $O(n^2)$ в худшем случае; это самый дорогой признак в наборе, и он
же определяет пропускную способность пайплайна (≈11 тыс. пар/с однопоточно).

**`name_token_jaccard`** — индекс Жаккара по токенам:

$$J(T_1, T_2) = \frac{|T_1 \cap T_2|}{|T_1 \cup T_2|}.$$

Дополняет предыдущий признак: инвариантен к порядку слов, но не видит опечаток
и морфологии. Пара $(\text{ratio}, J)$ разделяет два разных типа расхождений —
«те же слова в другом порядке» ($J$ высок, ratio ниже) и «похожие слова с
опечатками» (наоборот).

**`name_len_ratio`**, **`name_len_diff`**:

$$\frac{\min(|s_1|,|s_2|)}{\max(|s_1|,|s_2|,1)}, \qquad \bigl||s_1| - |s_2|\bigr|.$$

Прокси на «одна карточка — развёрнутое описание, вторая — краткое», типичный
паттерн для неполных дублей.

**`common_word_count`** — $|T_1 \cap T_2|$, ненормированный счётчик. В отличие от
$J$, сохраняет абсолютный масштаб: 8 общих слов из 10 и 2 из 2 дают разный
сигнал при одинаковом $J$ для второго случая.

### 2.2 Признаки по атрибутам

**`attr_key_jaccard`** — $J(K_1, K_2)$, насколько сопоставимы схемы атрибутов.

**`attr_common_key_count`** — $|K_1 \cap K_2|$.

**`attr_exact_match_ratio`** — доля совпадающих значений среди общих ключей:

$$\frac{\bigl|\{k \in K_1 \cap K_2 : A_1(k) = A_2(k)\}\bigr|}{|K_1 \cap K_2|}.$$

Самый прямой сигнал матча: совпадение по `артикул`, `партномер`, `oem-номер`
почти детерминированно означает дубль.

**`attr_value_jaccard`** — $J(V_1, V_2)$, устойчив к расхождению в именовании
ключей (когда один продавец пишет `бренд`, другой — `производитель`).

**`attr_count_diff`** — $\bigl||A_1| - |A_2|\bigr|$, полнота заполнения карточки.

**`category_match`** — $\mathbb{1}[c_1 = c_2]$.

### 2.3 Признаки, найденные разбором ошибок

Четыре признака добавлены не из общих соображений, а после чтения конкретных
ошибок на валидации (`src/analyze.py`).

**`category_code`** — код категории пары. Признак *согласованности* категорий
бесполезен (кандидаты всегда внутри одной, `category_match` тождественно ноль),
но измеренный ROC-AUC по категориям расходится от $0.67$ (Автотовары) до
$0.95$ (Аптека) — модель фактически решает двадцать разных задач, не зная,
какая перед ней. Объявляется категориальным, чтобы дерево делило по
произвольным подмножествам категорий, а не по бессмысленным порогам вида
«код < 7».

**`number_jaccard`, `number_count_diff`** — совпадение числовых токенов в
названиях. Почти каждая изученная ошибка упиралась в числа: диоптрии
$-2.00$ против $-3.00$, шторы $300{\times}220$ против $220$, РЦ $66\text{-}68$
против $68\text{-}70$. Токенные признаки такие различия топят: у пары очков,
отличающихся одной диоптрией, $J(T_1,T_2) = 0.636$, тогда как по числам —
$0.333$.

**`name_translit_ratio`** — сходство названий после свёртки кириллицы на
латиницу. Пара «кроссовки asics» / «кроссовки асикс» размечена как дубль, но
токенный Жаккар даёт $0.333$, поскольку общих токенов нет вовсе; после
транслитерации — $0.933$. Латинские и кириллические написания брендов в
каталоге сосуществуют повсеместно.

**`id_attr_match`** — совпадение по идентифицирующим атрибутам (`артикул`,
`партномер`, `oem-номер`, `модель`), с приведением к алфавитно-цифровому виду:
`8500892sx` и `8500892-SX` должны совпасть. NaN, если хотя бы у одной стороны
идентификатора нет — а это большинство карточек, и подстановка $0$ означала бы
ложное утверждение «проверили, различаются». Признак нацелен на Автотовары:
при добавлении cross-encoder'а эта категория выросла всего на $+0.005$ против
$+0.128$ у «Красоты и гигиены» — так выглядит subword-токенизация, дробящая
цифробуквенные коды в труху.

### 2.4 Семантика пропусков

Отдельный содержательный вопрос — что возвращать, когда признак **не определён**.
Наивная реализация возвращала $J(\varnothing, \varnothing) = 1$ («два пустых
множества идеально совпадают»), при этом `attr_exact_match_ratio` в аналогичной
ситуации возвращал $0$. То есть отсутствие информации кодировалось то как
максимум, то как минимум шкалы — и в первом случае модель получала
«идеальное совпадение» там, где сравнивать было нечего. Для карточек с пустыми
атрибутами это прямой генератор ложных срабатываний.

Текущая конвенция:

$$J(A, B) = \begin{cases}
\text{NaN}, & A = B = \varnothing \quad \text{(нет информации)} \\
0, & A = \varnothing \veebar B = \varnothing \quad \text{(реальное различие)} \\
\dfrac{|A \cap B|}{|A \cup B|}, & \text{иначе}
\end{cases}$$

`HistGradientBoostingClassifier` обрабатывает NaN нативно: на каждом сплите
пропуски отправляются в ту ветвь, которая даёт больший прирост, то есть
оптимальное направление **выучивается из данных**, а не задаётся руками.

Дополнительно введены индикаторы `attrs1_empty`, `attrs2_empty` —
$\mathbb{1}[|A| = 0]$. Пустота карточки становится наблюдаемым признаком, а не
маскируется под значение similarity.

---

## 3. Работа с двумя источниками разметки

Бинарная метка: $y = \mathbb{1}[t \geq 0.5]$.

Веса объектов:

$$w_i = \begin{cases}
\lambda_H, & i \in \text{human} \\
2\,|t_i - 0.5|, & i \in \text{LLM}
\end{cases}$$

Множитель $2|t - 0.5|$ отображает $[0,1] \to [0,1]$ с минимумом в $t = 0.5$:
уверенные суждения LLM ($t \to 0$ или $t \to 1$) получают полный вес, а
неуверенные — вес, стремящийся к нулю. По сути это линейная аппроксимация
уверенности классификатора-разметчика.

Пары с $t = 0.5$ **удаляются**, а не просто получают $w = 0$: правило
$y = \mathbb{1}[t \geq 0.5]$ пометило бы их как положительные, и они остались бы
в матрице как строки с меткой, не подкреплённой никаким сигналом. Нулевой вес
исключает их из функции потерь, но не из статистик разбиения и не из
`validation_fraction` внутри самого бустинга.

$\lambda_H$ не выбирается умозрительно, а перебирается по сетке
$\{1, 3, 5, 10\}$; выбирается значение с лучшим ROC-AUC на честной
валидации (§5).

---

## 4. Модель

`HistGradientBoostingClassifier` — гистограммный градиентный бустинг
(реализация в духе LightGBM). Минимизируется взвешенная логистическая потеря:

$$\mathcal{L} = -\sum_i w_i \bigl[y_i \log p_i + (1 - y_i)\log(1 - p_i)\bigr],
\qquad p_i = \sigma(F(x_i)).$$

Ансамбль строится аддитивно: $F_m(x) = F_{m-1}(x) + \nu\, h_m(x)$, где $\nu$ —
learning rate, а очередное дерево $h_m$ приближает ньютоновский шаг. Для
логистической потери градиент и гессиан по объекту:

$$g_i = w_i\,(p_i - y_i), \qquad h_i = w_i\,p_i(1 - p_i).$$

Оптимальное значение в листе $L$ при L2-регуляризации $\lambda$:

$$v_L^{*} = -\frac{\sum_{i \in L} g_i}{\sum_{i \in L} h_i + \lambda},$$

а прирост от разбиения узла на $L$ и $R$:

$$\text{Gain} = \frac{1}{2}\left[
\frac{(\sum_{L} g)^2}{\sum_{L} h + \lambda} +
\frac{(\sum_{R} g)^2}{\sum_{R} h + \lambda} -
\frac{(\sum_{L \cup R} g)^2}{\sum_{L \cup R} h + \lambda}
\right].$$

Здесь и виден механизм весов: $w_i$ входит и в $g_i$, и в $h_i$, то есть
масштабирует вклад объекта в выбор разбиения, а не только в итоговую потерю.

**Почему гистограммный вариант.** Признаки предварительно квантуются в 255
бинов, после чего поиск оптимального разбиения по признаку стоит $O(\#\text{bins})$
вместо $O(n \log n)$ на сортировку. При $n \approx 1.15 \times 10^7$ это
разница между часами и минутами. Дополнительно применяется приём вычитания
гистограмм: гистограмма дочернего узла получается вычитанием гистограммы
брата из родительской, что экономит проход по данным.

Гиперпараметры: `max_iter=500`, `learning_rate=0.05`, `max_depth=8`,
`l2_regularization=1.0`, ранняя остановка с `n_iter_no_change=20`.

---

## 5. Методология валидации

Это самая содержательная часть решения — здесь было исправлено две ошибки,
каждая из которых давала завышенную и потому бесполезную метрику.

### 5.1 Валидация только на ручной разметке

Изначально признаки human и LLM объединялись, после чего делался случайный
`train_test_split`. При соотношении $30{:}1$ валидационная выборка оказывалась
на $\approx 97\%$ размечена LLM. Формально метрика росла, но измеряла она
**согласие с LLM-разметчиком**, а не с истиной — при том что тест размечен
людьми.

Правильный порядок: hold-out выделяется из ручной разметки **до** какого-либо
смешивания, LLM-пары попадают исключительно в train, метрика считается только
на отложенной ручной части.

### 5.2 Утечка через связные компоненты

Пары не являются независимыми наблюдениями: они связаны через общие товары.
Если $(X, A)$ попала в train, а $(X, B)$ — в validation, модель на валидации
видит карточку, которую уже изучала. В товарном матчинге это усугубляется
кластерами почти идентичных карточек, порождающими десятки почти совпадающих
векторов признаков.

Формально: рассмотрим граф $G = (V, E)$, где $V$ — товары, $E$ — размеченные
пары. Разбиение должно резать не рёбра, а **связные компоненты**:

$$V = \bigsqcup_k C_k, \qquad
\text{train} = \bigcup_{k \in S} E[C_k], \quad
\text{val} = \bigcup_{k \notin S} E[C_k].$$

Компоненты строятся системой непересекающихся множеств (union-find) с сжатием
путей и объединением по рангу — амортизированная сложность
$O(|E|\,\alpha(|V|))$, где $\alpha$ — обратная функция Аккермана
(практически константа). Разбиение выполняется `GroupShuffleSplit` с
`groups = component_id`.

На ручной разметке: **345 654 компоненты на 365 654 пары** — то есть граф
распадается на множество мелких кластеров, а не на один гигантский. Поэтому
разбиение по компонентам остаётся близким к 80/20 (получилось 292 370 / 73 284).

Дополнительно закрыта та же утечка на стыке источников: из LLM-train
исключаются все пары, задевающие товары из отложенной ручной валидации.

### 5.3 Метрика: макро-среднее PR-AUC

Соревнование считает PR-AUC отдельно по каждой из 20 категорий и усредняет:

$$\text{score} = \frac{1}{20}\sum_{c=1}^{20} \text{PR-AUC}\bigl(y^{(c)}, \hat{p}^{(c)}\bigr).$$

Первые двадцать решений в этой работе принимались по общему ROC-AUC, что
неверно сразу по двум осям.

**Общее против макро-среднего.** В общей метрике категория весит
пропорционально числу пар, в макро-среднем все двадцать равны. Измеренному
общему PR-AUC $0.6388$ соответствовало макро $0.5648$. Практическое следствие:
улучшить слабейшую категорию стоит ровно столько же, сколько улучшить
сильнейшую, — а запаса в слабых кратно больше.

**ROC-AUC против PR-AUC.** ROC-AUC $= P(\hat{p}_+ > \hat{p}_-)$ инвариантна к
доле позитивов и слабо чувствительна к тому, сколько негативов стоит в самом
верху ранжирования. PR-AUC измеряет в основном именно это.

**Ловушка сравнения между выборками.** Нижняя граница PR-AUC равна доле
позитивов, поэтому значения несравнимы между наборами с разным балансом
классов. В нашей валидации доля $0.257$, и категории с редкими дублями дают
низкий PR-AUC даже при отличном ранжировании:

| Категория | Доля позитивов | PR-AUC | ROC-AUC | Превышение базы |
|---|---:|---:|---:|---:|
| Обувь | 0.087 | 0.443 | 0.872 | **5.07×** |
| Хобби и творчество | 0.471 | 0.883 | 0.907 | 1.88× |

Обувь ранжируется впятеро лучше случайного и вносит в метрику вдвое меньше,
чем Хобби, ранжирующееся вдвое лучше. Улучшать в таких категориях нужно не
общее качество, а точность в вершине списка.

Важности признаков считаются через **permutation importance** на отложенной
ручной валидации, а не через встроенные impurity-based важности — последние
систематически завышают вклад признаков с большой мощностью множества значений.

---

## 6. Результаты

Все числа — на одном и том же group-aware сплите, в hold-out только ручная
разметка.

### 6.1 Сводная траектория

| Конфигурация | ROC-AUC | pooled PR-AUC | macro PR-AUC |
|---|---:|---:|---:|
| GBM, строковые признаки | 0.7496 | 0.5273 | — |
| + MiniLM-L12 (без претрейна) | 0.8282 | 0.6388 | 0.5648 |
| + категория, числа, транслитерация, идентификаторы | 0.8468 | 0.6721 | — |
| + MiniLM-L12, претрейн 2M | 0.8961 | 0.7660 | 0.7060 |
| + MiniLM-L12, претрейн 11.19M | 0.9114 | 0.8008 | 0.7417 |
| **+ e5-base, претрейн 11.19M** | **0.9288** | **0.8367** | **0.7827** |

Два рычага дали почти весь прирост, и оба относятся к cross-encoder'у:

| Рычаг | На валидации | На лидерборде |
|---|---:|---:|
| Объём претрейна: 2M → 11.19M | +0.036 | +0.062 |
| Размер модели: 118M → 278M | +0.041 | +0.035 |

Строковые признаки при этом обесценились: над сильным CE вся надстройка GBM
добавляет $0.7809 \to 0.7827$, то есть $+0.0018$, занимая при этом 26% времени
инференса (§6.4).

### 6.2 Как использовать LLM-разметку

Это главный содержательный вывод работы, и он потребовал двух заходов.

**Что не работает: LLM-пары как обучающие строки.** Перебор веса ручной
разметки $\lambda_H$ при обучении GBM на объединении источников:

| $\lambda_H$ | ROC-AUC | PR-AUC |
|---|---:|---:|
| 1 | 0.7119 | 0.4780 |
| 3 | 0.7180 | 0.4867 |
| 5 | 0.7218 | 0.4922 |
| 10 | 0.7289 | 0.5017 |
| $\infty$ (= `--skip_llm`) | **0.7496** | **0.5273** |

Метрика растёт монотонно по мере подавления LLM-строк и достигает максимума
там, где они не влияют вовсе. То есть $\lambda_H$ не ищет оптимум внутри
диапазона, а интерполирует к краю — расширять сетку вверх бессмысленно.

**Что работает: LLM-разметка как отдельная стадия предобучения.** Cross-encoder
обучается на LLM-парах с мягкими метками, затем дообучается на ручной разметке.
Мягкие метки, а не бинаризованные: `BCEWithLogitsLoss` минимизируется при
$\sigma(z) = t$ для любого $t \in [0,1]$, поэтому максимально неуверенное
$t = 0.5$ само по себе почти не создаёт градиента — неопределённость
обрабатывается функцией потерь, а не внешним взвешиванием.

Объём претрейна оказался главным рычагом всей работы:

| Пар в претрейне | ROC-AUC после претрейна, **без единой ручной метки** | ROC-AUC итог |
|---:|---:|---:|
| без претрейна | — | 0.8077 |
| 1 млн | 0.7114 | 0.8720 |
| 2 млн | 0.7959 | 0.8957 |
| **11.19 млн** (всё, что есть) | **0.8548** | **0.9114** |

Прирост затухает — около $+0.085$ за первое удвоение и $+0.024$ на удвоение
дальше, — но в ноль не выходит: рычаг закрыт не насыщением, а тем, что данные
кончились.

**Почему два результата не противоречат друг другу.** Ключ — строка 0.7114:
модель, не видевшая ни одной ручной пары, уже почти догоняет GBM, обученный
именно на ней (0.7496). Значит переносимого сигнала в LLM-разметке много, и
губил её не собственный шум, а **способ смешивания**: 11 млн шумных строк и
365 тыс. чистых в одной функции потерь при соотношении $30{:}1$ — чистые
просто тонут. Отдельной стадией, поверх которой дообучение переписывает
решающий слой, та же разметка работает.

Косвенное подтверждение — в динамике эпох. Без претрейна модель упирается в
потолок на второй эпохе (0.8077) и на третьей идёт вниз (0.8074). С
претрейном на той же второй эпохе она ещё растёт: предобученное представление
позволяет извлечь из тех же 292 тыс. ручных пар заметно больше.

### 6.3 Калибровка валидации по лидерборду

Первая отправка дала $0.3626$ при валидационном макро $0.7060$ — падение вдвое,
которое сменой метрики не объяснялось. Чтобы понять причину, одна отправка была
потрачена на **диагностику**: то же решение без cross-encoder'а.

| Решение | Валидация | Лидерборд | Доля переноса |
|---|---:|---:|---:|
| Только строковые признаки | 0.5565 | 0.2650 | 0.476 |
| MiniLM, претрейн 2M | 0.7060 | 0.3626 | 0.514 |
| MiniLM, претрейн 11.19M | 0.7417 | 0.4245 | 0.570 |
| e5-base, претрейн 11.19M | 0.7827 | **0.4592** | **0.587** |

Два вывода.

**Разрыв — это масштаб, а не поломка.** Тестовые числа получаются из
валидационных примерно постоянным множителем. Наиболее вероятная причина —
меньшая доля позитивов в тесте: нижняя граница PR-AUC равна доле позитивов, и
при среднем превышении базы $3.44\times$ на нашей валидации результат $0.3626$
соответствует доле позитивов около $0.105$ против наших $0.257$. Порядок
моделей валидация предсказывает верно, абсолютные значения — в своей шкале.

**Валидация занижает каждое улучшение cross-encoder'а.** Доля переноса растёт
монотонно: $0.476 \to 0.514 \to 0.570 \to 0.587$. И относительный прирост на
лидерборде каждый раз обгоняет валидационный:

| Шаг | На валидации | На лидерборде |
|---|---:|---:|
| претрейн 2M → 11M | +5.5% | **+17%** |
| MiniLM → e5-base | +5.5% | **+8.2%** |

Объяснение в устройстве валидации. Она собрана из товаров `items_human` —
того же узкого подмножества (711 тыс. из 13.4 млн), на котором идёт
дообучение. Претрейн же покрывает весь каталог, а более крупная модель лучше
обобщает — и то и другое проявляется именно на **незнакомых** товарах, которых
в нашей валидации нет, а тест состоит из них целиком.

Практическое следствие для принятия решений: скромный валидационный прирост от
усиления cross-encoder'а следует читать как заметный прирост на тесте. По этому
направлению надо давить, а не осторожничать.

Практическое следствие: скромный прирост от претрейна на валидации следует
читать как заметный прирост на тесте, и решение об отправке принимать с этой
поправкой.

### 6.4 Композиция

Прирост от надстройки GBM над cross-encoder'ом асимметричен: на слабом CE
ROC-AUC давал $+0.027$, а PR-AUC $+0.060$ — вдвое больше в относительном
выражении. При доле позитивов $0.257$ именно PR-AUC отражает практическую
полезность.

С усилением CE вклад надстройки закономерно сжимается: над CE v3 она добавляет
$0.8720 \to 0.8793$, то есть $+0.007$. Причина видна в важностях — строковые
признаки уступают свою долю по мере того, как CE перекрывает их сигнал.
Наблюдалось это и раньше: улучшение CE на $+0.007$ дало композиции лишь
$+0.001$.

Разрыв между train и val у cross-encoder'а — $0.8292$ против $0.8003$, всего
$0.029$. Это и определило способ построения надстройки: при слабом запоминании
train'а логит CE можно подавать в GBM напрямую. Иначе потребовалась бы
out-of-fold-схема — $K$ отдельных CE, каждый размечает свой отложенный фолд,
— поскольку GBM, видя на train неестественно хорошие скоры, переоценил бы
признак. (На честность итоговой метрики это не влияет — val не видели ни CE,
ни GBM, — но модель вышла бы неоптимальной.)

Разрыв между train и val у cross-encoder'а — $0.8292$ против $0.8003$, всего
$0.029$. Это и определило способ построения надстройки: при слабом запоминании
train'а логит CE можно подавать в GBM напрямую. Иначе потребовалась бы
out-of-fold-схема — $K$ отдельных CE, каждый размечает свой отложенный фолд,
— поскольку GBM, видя на train неестественно хорошие скоры, переоценил бы
признак. (На честность итоговой метрики это не влияет — val не видели ни CE,
ни GBM, — но модель вышла бы неоптимальной.)

### 6.5 Важности признаков

Три среза, показывающие, как перераспределялся вклад:

| Признак | только строки | + CE v2 | + CE v3 (претрейн) |
|---|---:|---:|---:|
| `ce_score` | — | 0.2716 | **0.3539** |
| `number_jaccard` | — | — | 0.0023 |
| `category_code` | — | — | 0.0033 |
| `name_token_jaccard` | 0.1337 | 0.0270 | 0.0026 |
| `common_word_count` | 0.0965 | 0.0140 | 0.0006 |
| `attr_common_key_count` | 0.0382 | 0.0019 | 0.0006 |
| `name_seq_ratio` | 0.0316 | 0.0084 | 0.0017 |
| `attr_value_jaccard` | 0.0237 | 0.0099 | 0.0020 |
| `category_match`, `attrs1_empty`, `attrs2_empty` | 0.0000 | 0.0000 | 0.0000 |

`category_match` тождественно нулевой во всех конфигурациях — кандидаты
генерируются внутри категории, признак константен. Полезна не *согласованность*
категорий, а то, *какая* это категория (`category_code`, §2.3).

Главное в этой таблице — не отдельные числа, а тенденция: по мере усиления
cross-encoder'а строковые признаки последовательно уступают ему свою долю, и
над CE v3 все они опускаются до уровня шума ($\leq 0.003$). Коллективно они всё
ещё дают $+0.007$, но модель уже почти целиком опирается на один признак.

---

## 7. Бюджет инференса и cross-encoder

### 7.1 Ревизия исходной посылки

Пайплайн строился под тезис «скорость — жёсткое ограничение, поэтому только
дешёвые признаки». Опубликованные лимиты этот тезис опровергают:

| Ресурс среды оценки | |
|---|---|
| CPU | 20 ядер |
| RAM | 200 ГБ |
| GPU | **NVidia H100 80 ГБ** |
| Лимит Check / Public / Private | 1 / 6 / **13** мин |
| Архив решения / docker-образ | 5 ГБ / 15 ГБ |

Замер текущего решения на объёме, сопоставимом с тестовым (365 тыс. пар,
711 тыс. товаров): загрузка каталога + признаки + GBM укладываются
в **~60–90 с** против лимита в **780 с**, при полностью простаивающем GPU.

То есть оптимизация шла по ресурсу, которого в избытке, а результат
(ROC-AUC 0.75 на строковых эвристиках) упирался в выразительность признаков.
Порядок величины запаса — примерно $10\times$ по времени плюс неиспользованный
H100.

**Измерено** на распакованном архиве решения — то есть ровно то, что запустит
система оценки. e5-base, 365 654 пары, H100, при этом карта была занята
параллельным обучением, так что это оценка **сверху**:

| Этап | Время | Доля |
|---|---:|---:|
| Загрузка каталога и пар | 4.7 с | 1% |
| Cross-encoder | 241.5 с | 72% |
| Строковые признаки | 86.2 с | 26% |
| GBM и запись | ~1 с | — |
| **Итого** | **333 с** | против лимита 780 |

Отсюда два практических вывода.

**Строковые признаки стали невыгодны.** Они занимают 26% времени и дают
$+0.0018$ к метрике. Пока запас есть — пусть работают, но это первое, что
следует снять, если время станет дефицитом.

**Оценивать стоимость модели надо по вычислениям, а не по числу параметров.**
У multilingual-моделей словарь в 250 тыс. токенов, и эмбеддинги съедают большую
часть параметров, не требуя вычислений:

| | Слоёв | Размерность | Параметров всего | В слоях (это и считается) |
|---|---:|---:|---:|---:|
| MiniLM-L12 | 12 | 384 | 118M | ~21M |
| e5-base | 12 | 768 | 278M | ~85M |
| e5-large | 24 | 1024 | 560M | ~302M |

Между e5-base и e5-large разница по параметрам двукратная, а по вычислениям
($\text{слоёв} \times \text{размерность}^2$) — **в 3.5 раза**. Соответственно и
время обучения, и время инференса растут в 3.5 раза, а не вдвое.

Веса модели (490 МБ у MiniLM, 1.1 ГБ у e5-base) свободно укладываются в лимиты
архива (5 ГБ) и образа (15 ГБ). Модель обязана лежать **внутри архива**: в
среде оценки нет сети, и загрузка с HF Hub в рантайме там не сработает.

### 7.2 Почему cross-encoder, а не bi-encoder

Bi-encoder кодирует товары независимо и считает косинус; cross-encoder подаёт
оба текста в одну последовательность через `[SEP]` и видит их взаимодействие
на каждом слое внимания.

Обычный аргумент в пользу bi-encoder — переиспользование: эмбеддинг товара
считается один раз и участвует во многих парах. Здесь этот аргумент не
работает. При 365 тыс. пар и 711 тыс. товаров каждый товар встречается в
среднем

$$\frac{2 \times 365\,654}{711\,304} \approx 1.03 \text{ раза},$$

то есть переиспользования практически нет. Bi-encoder потребовал бы 711 тыс.
прямых проходов, cross-encoder — 365 тыс. (пусть и на вдвое длинных
последовательностях), при заметно более высокой точности. Выбор однозначен.

### 7.3 Как встроен

Cross-encoder — `AutoModelForSequenceClassification` с одним логитом,
обучается на бинарной кросс-энтропии на **том же** train-сплите, что и GBM
(`src/split.py` — единственный источник истины для разбиения). Его логит
добавляется как ещё одна колонка признаков, поверх — тот же GBM.

Такая композиция, а не замена GBM на CE, потому что признаки дополняют друг
друга: точные совпадения по `артикул`/`oem-номер` — это ровно тот сигнал,
который символьная модель ловит надёжнее, чем трансформер с ограниченным
контекстом и агрессивной токенизацией цифробуквенных кодов.

Пример из данных, объясняющий, зачем вообще нужна семантика:

| Товар A | Товар B | Строковое сходство |
|---|---|---|
| очки … romeo 1.56 hmc/emi **−2.00** рц 62-64 | **фотохромные** очки … romeo 1.56 **−3.00** рц 64-66 | очень высокое |
| зубная щетка r.o.c.s. **black** edition classic | зубная щетка r.o.c.s. **red** edition classic | очень высокое |
| сандалии fre gamo, цвет **чёрный** | fre gamo / сандалии, цвет **жёлтый** | очень высокое |

Все три пары почти наверняка **не** дубли, но различаются одним-двумя токенами
внутри длинного общего текста. Жаккар и Рэтклифф—Обершелп здесь дают около
единицы; различить их можно только понимая, что диоптрии, цвет и модификация —
это идентифицирующие атрибуты, а не шум.

---

## 8. Что дальше

Сделано и закрыто: претрейн на LLM-разметке (§6.2, крупнейший прирост),
признаки из разбора ошибок (§2.3), переход на метрику соревнования (§5.3),
калибровка по лидерборду (§6.3).

**Что осталось по линии LLM-разметки.** Объём исчерпан — использованы все
11.19 млн пар. Остаются способы выжать больше из тех же данных:
- **вторая эпоха претрейна**: сейчас каждая пара увидена ровно один раз;
- **более крупная модель**: `multilingual-e5-base` (278M против 118M). На H100
  она замедляет обучение лишь в 1.9 раза при трёхкратном числе параметров —
  MiniLM с размерностью 384 карту недогружает, а 768 использует её эффективнее;
- **отбор по согласованности**: оставить те LLM-пары, где модель, обученная на
  ручной разметке, согласна с меткой LLM;
- **калибровка**: оценить $P(y = 1 \mid t)$ эмпирически по пересечению с ручной
  разметкой, вместо допущения, что $t$ — уже вероятность.

**По линии метрики.** Метрика усредняет PR-AUC по категориям, а слабейшие
категории слабы не из-за плохого ранжирования: у Обуви ROC-AUC $0.872$ при
PR-AUC $0.443$. Улучшать там нужно точность в вершине списка, а не общее
качество — то есть калибровку порогов по категориям и признаки, дающие
уверенное «нет» (различающиеся артикулы при похожих названиях; сейчас
`id_attr_match` отвечает только за совпадение).

**По линии признаков:**
- `category_match` удалить как константу (тождественно нулевой по permutation
  importance во всех конфигурациях);
- TF-IDF косинус с IDF-весами по каталогу: редкие токены (артикулы, модели)
  должны весить больше частотных (`для`, `шт`, `комплект`);
- числовые признаки из названий с разбором единиц измерения — сейчас числа
  сравниваются как строки, без понимания, что $0.5$ л и $500$ мл совпадают.

**По линии надёжности выводов.** Все решения принимались по одному сплиту
(`SPLIT_SEED = 1234`), и таких решений набралось более двадцати. Крупные
скачки шумом не объяснить, но различия в пределах $\pm 0.005$ между близкими
кандидатами без проверки на втором сиде доверия не заслуживают.

---

## 9. Структура и запуск

```
matching_gbm/
├── run.py                 # инференс, CLI-контракт соревнования
├── metadata.json          # docker image + entry point
├── requirements.txt
└── src/
    ├── features.py        # признаки на пару + текст для энкодера (§2)
    ├── pipeline.py        # загрузка каталога, чанкинг, union-find (§5.2)
    ├── split.py           # общий group-aware сплит для GBM и CE (§5.2)
    ├── train.py           # разбиение, перебор λ_H, обучение GBM (§3–6)
    ├── ce_train.py        # дообучение cross-encoder (§7)
    └── ce_score.py        # скоринг пар cross-encoder'ом (§7)
```

**Обучение GBM.** Учитывая §6, базовая конфигурация — только ручная разметка:

```bash
python3 -m src.train \
  --items_human_path items_human.parquet \
  --matches_path matches.parquet \
  --output_model_path gbm_model.joblib \
  --skip_llm
```

**Обучение cross-encoder** (валидируется на том же сплите, что и GBM):

```bash
python3 -m src.ce_train \
  --items_human_path items_human.parquet \
  --matches_path matches.parquet \
  --output_dir ce_model \
  --batch_size 64 --epochs 2
```

**GBM поверх CE.** Сначала скоринг, затем обучение с `ce_score` как признаком:

```bash
python3 -m src.ce_score \
  --items_path items_human.parquet \
  --matches_path matches.parquet \
  --ce_model_dir ce_model \
  --output_path ce_scores.npy

python3 -m src.train \
  --items_human_path items_human.parquet \
  --matches_path matches.parquet \
  --ce_scores_path ce_scores.npy \
  --output_model_path gbm_model.joblib \
  --skip_llm
```

**Инференс.** `run.py` подхватывает `ce_model/`, если директория существует,
и работает на одних строковых признаках, если её нет:

```bash
python3 run.py \
  --items_path <test_items.parquet> \
  --matches_path <test_matches.parquet> \
  --output_path submission.csv
```

### Инженерные замечания

**Выравнивание выхода.** `build_feature_matrix` возвращает ровно
`len(match_df)` строк в исходном порядке. Пары, где товар отсутствует в
каталоге, получают строку из NaN, но **не удаляются** — иначе предсказания
съезжают относительно `id1`/`id2`, и в сабмит попадает меньше строк, чем во
входном файле.

**Память.** Каталог хранится как словарь `id → (name, attributes, category)`
с сырыми строками; разбор JSON и токенизация выполняются лениво, на уровне
чанка в 200 тыс. пар, с кэшем, живущим только в пределах чанка. Ранняя версия
подготавливала все 12.4 млн товаров заранее и упиралась в лимит контейнера
(85 ГБ) — при 13 млн товаров тяжёлые Python-объекты (`frozenset`, `dict`)
дают многократный оверхед поверх исходных данных.

**Почему однопоточно.** Попытка распараллелить через `multiprocessing.Pool` с
`fork` выглядела бесплатной (copy-on-write должен был разделить каталог между
процессами), но в CPython любое обращение к объекту инкрементирует счётчик
ссылок — то есть является **записью**. Страницы «разделяемого» словаря
копируются при чтении, воспроизводя ровно ту утечку памяти, которой fork должен
был избежать; при большом RSS родителя сам `fork` дополнительно деградирует на
копировании таблиц страниц. Реальное распараллеливание этого шага требует
данных в настоящей разделяемой памяти (Arrow IPC / `shared_memory`), а не
обычных объектов Python.

**Единый сплит.** GBM и cross-encoder обязаны валидироваться на побайтово
одинаковых отложенных парах, иначе их метрики несравнимы, а при объединении
выходов пара, отложенная у одной модели, но обученная у другой, протекает.
Поэтому разбиение вынесено в `src/split.py` как единственный источник истины,
а не воспроизводится в каждом скрипте.

**Согласованность CE-скоров.** `--ce_scores_path` принимает `.npy`,
выровненный построчно с файлом разметки **как он лежит на диске**. Длина
проверяется явно: устаревший файл скоров, посчитанный по другой или уже
отфильтрованной выборке, иначе сдвинул бы все значения на неизвестное
смещение — молча, без ошибки. По той же причине фильтрация по
`exclude_items` применяется уже **после** построения признаков.

**Ленивый импорт torch.** `run.py` импортирует `src.ce_score` только при
наличии `ce_model/`. При лимите стадии Check в одну минуту фиксированные
издержки (`import torch`, инициализация CUDA, загрузка весов) — заметная доля
бюджета, и решение без cross-encoder'а не должно их платить.

**Проверка совместимости.** Перед предсказанием сверяется `n_features_in_`
модели с шириной матрицы признаков: рассинхрон GBM и `ce_model/` (например,
GBM обучен с `ce_score`, а директория с весами не попала в архив) падает с
внятным сообщением, а не выдаёт молча неверные предсказания.
